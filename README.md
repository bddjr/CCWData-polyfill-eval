将 “Gandi云数据” 扩展的 `getValueInJSON` 和 `setValueInJSON` 函数回滚到修复前的行为。

构建：

```
git clone https://github.com/bddjr/CCWData-polyfill-eval
cd CCWData-polyfill-eval
pnpm i
pnpm build
```

构建后的文件在 `dist` 文件夹。

源代码在 `src/KEY.js` 。
